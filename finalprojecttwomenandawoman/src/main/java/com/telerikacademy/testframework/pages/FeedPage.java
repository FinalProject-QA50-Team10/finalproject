package com.telerikacademy.testframework.pages;

import org.openqa.selenium.WebDriver;

public class FeedPage extends BasePage {
    public FeedPage(WebDriver driver, String urlKey) {
        super(driver, urlKey);
    }
}
